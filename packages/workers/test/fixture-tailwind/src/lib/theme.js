// Reached only through the page's frontmatter, so its stylesheet only lands in the
// bundle if the collector walks the JS graph transitively.
import "../styles/extra.css";

export const TAGLINE = "utilities from a virtual file map";
