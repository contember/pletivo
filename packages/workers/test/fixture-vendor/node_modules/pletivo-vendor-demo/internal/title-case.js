// Deep enough that the entry alone is not the whole package: a vendored module has to
// arrive bundled, not copied.
export function titleCase(word) {
  return word.charAt(0).toUpperCase() + word.slice(1);
}
