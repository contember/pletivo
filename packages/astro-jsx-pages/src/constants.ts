/**
 * Shared constants for the TSX pages plugin.
 */

/**
 * The renderer name used to identify Astro's JSX runtime.
 * Used to tag components so Astro knows which renderer to use.
 */
export const ASTRO_JSX_RENDERER = 'astro:jsx';
