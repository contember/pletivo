{
  // Same program the repo's `bun run typecheck` compiles this example in —
  // duplicating the options here drifted (and pulled in a second copy of
  // bun-types from this example's own node_modules).
  "extends": "../../tsconfig.json",
  "include": ["src"]
}
